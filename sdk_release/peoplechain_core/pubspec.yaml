name: peoplechain_core
description: On-device, decentralized chat ledger (PeopleChain) for Flutter/Dart apps.
version: 0.1.0
homepage: https://example.com/peoplechain
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cryptography: ^2.0.0
  uuid: any
  isar: ^3.1.0
  isar_flutter_libs: ^3.1.0
  path_provider: ^2.1.4
  flutter_webrtc: ^1.0.0
  multicast_dns: 0.3.3
  flutter_secure_storage: ^9.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.0.0

flutter:
  uses-material-design: false
